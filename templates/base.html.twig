<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js" integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI" crossorigin="anonymous"></script>
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body class="d-flex flex-column min-vh-100">
        <section class="d-flex justify-content-center position-relative" style="margin-bottom: 0;">
            <nav class="navbar navbar-expand-lg navbar-dark bg-dark w-100">
            <div class="container-fluid">
            <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
            <a class="navbar-brand" href="{{ path('app_home') }}" style="font-weight: 600; color: #fff;">Home</a>
                <li class="nav-item">
                <a class="nav-link" href="#" style="font-weight: 600; color: #fff;">Catalog</a>
                </li>
                <li class="nav-item">
                <a class="nav-link" href="#" style="font-weight: 600; color: #fff;">About Us</a>
                </li>
                <li class="nav-item">
                <a class="nav-link" href="#" style="font-weight: 600; color: #fff;">Contact</a>
                </li>
            </ul>
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                <a class="nav-link" href="{{ path('app_logout') }}" style="font-weight: 600; color: #fff;">Logout</a>
                </li>
            </ul>
            {% else %}
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                <a class="nav-link" href="{{ path('app_login') }}" style="font-weight: 600; color: #fff;">Login</a>
                </li>
                <li class="nav-item">
                <a class="nav-link" href="{{ path('app_register') }}" style="font-weight: 600; color: #fff;">Register</a>
                </li>
            </ul>
            {% endif %}
            </div>
            </div>
            </nav>
        </section>
        <div class="position-relative" style="height: 30vh; margin: 0;">
            <video autoplay muted loop id="myVideo" class="w-100" style="height: 100%; object-fit: cover; filter: brightness(0.5);">
            <source src="{{ asset('images/ps4.mp4') }}" type="video/mp4">
            </video>
            <div class="position-absolute top-50 start-50 translate-middle">
            <h1 style="color: white; font-size: 3rem; font-weight: bold;">OnlineGameStore</h1>
            </div>
        </div>
        <div class="container">
            {% block body %}{% endblock %}
        </div>
        <footer class="bg-dark text-white text-center py-3 mt-auto">
            &copy; {{ "now"|date("Y") }} OnlineGameStore. All rights reserved.
        </footer>
    </body>
</html>
